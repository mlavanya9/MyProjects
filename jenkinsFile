#!groovy
pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				echo 'Building'
			}
		}
		stage('Test') {
			steps {
				echo 'testing'
				sh 'pwd'
				sh 'ls'
			}
		}
		
		stage('prep') {
			steps {
				echo 'testing'
				sh 'pwd'
				sh 'ls'
			}
		}
		
	}
}
